numpy
opencv-python
pycuda
pyttsx3
pytesseract
torch
torchvision

tensorrt

sudo docker run --rm -it \
  --runtime nvidia \
  --device /dev/video0 \
  --device /dev/snd \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -e DISPLAY=$DISPLAY \
  --privileged \
  my-jetson-container
